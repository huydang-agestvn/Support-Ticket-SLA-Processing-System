openapi: 3.0.0
info:
  title: Support Ticket SLA Processing System API
  version: 1.0.0
  description: |
    RESTful API contract for the Internal Support Ticket & SLA Processing System. 
    This system manages the entire lifecycle of internal support tickets (IT, HR, Facilities, etc.) and tracks compliance with Service Level Agreements (SLAs).

    ### Key Features:
    * **Ticket Lifecycle Management**: Implements a strict Finite State Machine (FSM) state transition workflow.
    * **High-Throughput Batch Import**: Supports concurrent batch processing of historical ticket events via an in-memory worker pool, with comprehensive validation and automated audit logging to object storage (MinIO) for rejected events.
    * **Automated SLA Reporting**: Features an ETL pipeline that aggregates operational metrics, resolution times, and SLA breaches daily via scheduled cron jobs or CLI triggers.
    * **Enterprise Authentication & RBAC**: Secured via Keycloak Identity Provider (OAuth2/JWT) enforcing three functional roles: Requestor, Agent, and Manager.

    ### Response Standardization
    All responses adhere to a consistent wrapper format: `{ "success": boolean, "message": string, "data": object, "error": object }`.

servers:
  - url: http://localhost:8080/api/v1
    description: Local development server

tags:
  - name: Auth
    description: Authentication and token management APIs integrated with Keycloak Identity Provider.
  - name: Tickets
    description: Core ticket operations including creation, pagination, detailed lookup, and state machine transitions.
  - name: Ticket Events
    description: High-throughput asynchronous batch processing for importing historical ticket event trails.
  - name: Reports
    description: Operational SLA dashboards and daily metrics aggregation for management.
  - name: AI Assistant
    description: AI-powered analytical, automated triage, model evaluation metrics, and descriptive query assistants for support tickets.

paths:
  /auth/login:
    post:
      tags:
        - Auth
      summary: Authenticate user credentials
      description: Exchanges a username and password for a Keycloak OAuth2 access token. This endpoint does not require authentication headers.
      operationId: login
      requestBody:
        $ref: "#/components/requestBodies/LoginRequestBody"
      responses:
        "200":
          $ref: "#/components/responses/LoginSuccess"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/InvalidCredentials"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "503":
          $ref: "#/components/responses/AuthProviderUnavailable"

  /tickets:
    post:
      tags:
        - Tickets
      summary: Create a new support ticket
      description: |
        Initializes a new support ticket in the default `new` state. 
        The `requestor_id` is automatically extracted from the authenticated user's JWT payload.
        
        **Required Roles**: Requestor, Manager.
      operationId: createTicket
      security:
        - OAuth2Password: []
      requestBody:
        $ref: "#/components/requestBodies/CreateTicketRequestBody"
      responses:
        "201":
          $ref: "#/components/responses/CreateTicketSuccess"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

    get:
      tags:
        - Tickets
      summary: List and filter tickets
      description: |
        Retrieves a paginated list of support tickets. Supports advanced server-side filtering by status, priority, and assignee.
        
        **Data Visibility Constraints**:
        * **Requestor**: Can only view tickets they personally created.
        * **Agent / Manager**: Can view all tickets across the system.
        
        **Required Roles**: Requestor, Agent, Manager.
      operationId: listTickets
      security:
        - OAuth2Password: []
      parameters:
        - $ref: "#/components/parameters/TicketStatusQuery"
        - $ref: "#/components/parameters/TicketPriorityQuery"
        - $ref: "#/components/parameters/AssigneeIdQuery"
        - $ref: "#/components/parameters/PageQuery"
        - $ref: "#/components/parameters/LimitQuery"
      responses:
        "200":
          $ref: "#/components/responses/ListTicketsSuccess"
        "400":
          $ref: "#/components/responses/InvalidQueryParameters"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /tickets/{id}:
    get:
      tags:
        - Tickets
      summary: Retrieve ticket details
      description: |
        Fetches the complete details of a specific ticket by its ID, including its associated historical state transitions (Ticket Events audit log).
        
        **Data Visibility Constraints**:
        * **Requestor**: Access restricted to their own tickets.
        * **Agent / Manager**: Unrestricted access to all tickets.
        
        **Required Roles**: Requestor, Agent, Manager.
      operationId: getTicketById
      security:
        - OAuth2Password: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          $ref: "#/components/responses/GetTicketSuccess"
        "400":
          $ref: "#/components/responses/InvalidTicketId"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/TicketNotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /tickets/{id}/status:
    patch:
      tags:
        - Tickets
      summary: Update ticket status (FSM Transition)
      description: |
        Updates the operational status of an existing ticket. This endpoint strictly enforces the Finite State Machine (FSM) rules:
        * `new` → `assigned`, `cancelled` (requires `assignee_id` when moving to `assigned`).
        * `assigned` → `in_progress`, `cancelled`.
        * `in_progress` → `resolved`.
        * `resolved` → `closed`.
        
        Terminal states (`closed`, `cancelled`) cannot transition further. Each successful change logs a record into the database audit trail within a single transaction.
        
        **Required Roles**: Agent, Manager.
      operationId: updateTicketStatus
      security:
        - OAuth2Password: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      requestBody:
        $ref: "#/components/requestBodies/UpdateTicketStatusRequestBody"
      responses:
        "200":
          $ref: "#/components/responses/UpdateTicketStatusSuccess"
        "400":
          $ref: "#/components/responses/InvalidStatusTransition"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/TicketNotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /ticket-events/import:
    post:
      tags:
        - Ticket Events
      summary: Asynchronously batch import ticket events
      description: |
        Imports a large batch of historical state transition logs. Accepts either a multipart file upload (`.csv` or `.json`) or a raw JSON array.
        
        Processing utilizes an in-memory concurrent worker pool to model and validate state machines concurrently per ticket. Valid logs are committed in bulk transactions, while validation failures generate a detailed CSV error report streamed directly to MinIO storage.
        
        **Required Roles**: Agent, Manager.
      operationId: importTicketEvents
      security:
        - OAuth2Password: []
      requestBody:
        $ref: "#/components/requestBodies/ImportTicketEventsRequestBody"
      responses:
        "200":
          $ref: "#/components/responses/ImportTicketEventsSuccess"
        "400":
          $ref: "#/components/responses/InvalidImportInput"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /reports/daily:
    get:
      tags:
        - Reports
      summary: Get daily SLA performance report
      description: |
        Retrieves consolidated SLA tracking metrics and KPIs for a specific date. Data includes volume counts, priority distributions, and average resolution times compiled via the ETL pipeline.
        
        **Required Roles**: Manager.
      operationId: getDailyReport
      security:
        - OAuth2Password: []
      parameters:
        - $ref: "#/components/parameters/ReportDateQuery"
      responses:
        "200":
          $ref: "#/components/responses/GetDailyReportSuccess"
        "400":
          $ref: "#/components/responses/InvalidDateFormat"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "504":
          description: Report not found for the requested date.
        "500":
          $ref: "#/components/responses/InternalServerError"

  /ai/tickets/{id}/triage:
    post:
      tags:
        - AI Assistant
      summary: Perform AI-driven triage and SLA risk analysis for a single ticket
      description: |
        Executes automated category classification (such as IT, HR, Facilities), evaluates urgency levels, predicts potential SLA breach risks, and provides tailored recommended next actions for a specific ticket identifier.
        
        The result integrates deterministic system rules with generative evaluation data.
        
        **Required Roles**: Agent, Manager.
      operationId: triageSingleTicket
      security:
        - OAuth2Password: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          description: AI triage analysis compiled and structured successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AITriageResponse"
        "400":
          $ref: "#/components/responses/AITriageSingleBadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/AITriageTicketNotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /ai/tickets/triage:batch:
    post:
      tags:
        - AI Assistant
      summary: Dispatch bulk ticket triage and SLA breach risk scanning
      description: |
        Allows system administrators (Support Leads or Managers) to process a large batch of open (non-closed and non-cancelled) support tickets.
        
        The in-memory worker pool evaluates the collections concurrently to detect early indicators of imminent SLA breaches, enabling optimized, preemptive resource reallocation and workload adjustments.
        
        **Required Roles**: Manager.
      operationId: triageBatchTickets
      security:
        - OAuth2Password: []
      requestBody:
        $ref: "#/components/requestBodies/AIBatchTriageRequestBody"
      responses:
        "200":
          description: Batch triage matrix analysis dispatched and evaluated successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIBatchTriageResponse"
        "400":
          $ref: "#/components/responses/AIBatchTriageBadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /ai/tickets/{id}/triage/latest:
    get:
      tags:
        - AI Assistant
      summary: Fetch the latest stored AI triage results for a ticket
      description: |
        Provides pre-existing AI analytical triage data compiled from the most recent assessment job sequence for a specific ticket identifier.
        
        This drastically minimizes external LLM operational token costs, bypasses redundant upstream payload generation cycles, and optimizes server network response latency limits.
        
        **Required Roles**: Agent, Manager.
      operationId: getLatestTicketTriage
      security:
        - OAuth2Password: []
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          description: Previously persisted AI triage analytical records retrieved successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AITriageResponse"
        "400":
          $ref: "#/components/responses/AITriageSingleBadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/AITriageTicketNotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /ai/evaluations/ticket-triage:
    post:
      tags:
        - AI Assistant
      summary: Execute an automated batch evaluation run for ticket triage datasets
      description: |
        Run a batch evaluation against predefined ground-truth test cases for the ticket triage AI workflow.
        
        Returns classification accuracy metrics, latency stats, throughput, and a per-case breakdown comparing AI predictions against expected labels.

        Use this endpoint to validate prompt changes and verify fallback behavior before deploying to production.
        
        **Required Roles**: Manager.
      operationId: runTriageEvaluation
      security:
        - OAuth2Password: []
      requestBody:
        $ref: "#/components/requestBodies/AIEvaluationRequestBody"
      responses:
        "200":
          description: Evaluation matrix sequence metrics finalized and parsed successfully.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AIEvaluationResponse"
        "400":
          $ref: "#/components/responses/AIEvaluationBadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"

components:
  securitySchemes:
    OAuth2Password:
      type: oauth2
      flows:
        password:
          tokenUrl: http://localhost:8080/api/v1/auth/login
          scopes: {}

  parameters:
    TicketId:
      name: id
      in: path
      required: true
      description: The unique auto-incrementing database identifier of the support ticket.
      schema:
        type: integer
        minimum: 1
      example: 9

    TicketStatusQuery:
      name: status
      in: query
      required: false
      description: Filters the results matching a specific lifecycle status.
      schema:
        $ref: "#/components/schemas/TicketStatus"

    TicketPriorityQuery:
      name: priority
      in: query
      required: false
      description: Filters the results matching an SLA urgency classification level.
      schema:
        $ref: "#/components/schemas/TicketPriority"

    AssigneeIdQuery:
      name: assignee_id
      in: query
      required: false
      description: Filters tickets assigned to a specific processing agent using their Keycloak UUID string format.
      schema:
        type: string
      example: 9ee00625-0436-4f11-8bb1-b9e4f3f7bf88

    PageQuery:
      name: page
      in: query
      required: false
      description: Page index number for server-side pagination.
      schema:
        type: integer
        minimum: 1
        default: 1
      example: 1

    LimitQuery:
      name: limit
      in: query
      required: false
      description: Maximum number of ticket items to return per page.
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 10
      example: 10

    ReportDateQuery:
      name: date
      in: query
      required: false
      description: Target date partition for the compiled report schema. Must follow the standard `YYYY-MM-DD` date layout.
      schema:
        type: string
        format: date
      example: "2026-05-19"

  requestBodies:
    LoginRequestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/LoginRequest"
          example:
            username: requestor-1
            password: "123123"

    CreateTicketRequestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CreateTicketRequest"
          example:
            requestor_id: 0a5389df-ba3a-4494-a095-126d05c7c2e7
            title: Cannot access internal system
            description: User cannot access internal support system
            priority: low
            sla_due_at: "2026-05-25T15:57:38.248399+07:00"

    UpdateTicketStatusRequestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/UpdateTicketStatusRequest"
          example:
            status: in_progress
            note: Start processing ticket

    ImportTicketEventsRequestBody:
      required: true
      content:
        multipart/form-data:
          schema:
            $ref: "#/components/schemas/TicketEventImportFileRequest"
        application/json:
          schema:
            $ref: "#/components/schemas/TicketEventImportJsonRequest"
          example:
            - ticket_id: 19
              note: Ticket assigned to support agent
              from_status: new
              to_status: assigned
              assignee_id: 9ee00625-0436-4f11-8bb1-b9e4f3f7bf88
              created_at: "2026-05-30T17:59:49.129094074+07:00"
            - ticket_id: 19
              note: Ticket moved to in progress
              from_status: assigned
              to_status: in_progress
              assignee_id: 9ee00625-0436-4f11-8bb1-b9e4f3f7bf88
              created_at: "2026-06-01T17:59:49.129094074+07:00"

    AIBatchTriageRequestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AIBatchTriageRequest"
          example:
            ticket_ids: [455, 456, 457, 458]

    AIEvaluationRequestBody:
      required: true
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AIEvaluationRequest"
          example:
            prompt_version: "v2.1.0-triage-assistant"
            evaluation_case_ids: [1, 2, 3, 4, 5, 12, 20]

  responses:
    LoginSuccess:
      description: Authentication successful. Keycloak tokens granted.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/LoginSuccessResponse"
          example:
            success: true
            message: Login successfully
            data:
              access_token: eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...
              token_type: Bearer
              expires_in: 300

    CreateTicketSuccess:
      description: Support ticket compiled and stored successfully.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CreateTicketResponse"

    ListTicketsSuccess:
      description: Paginated query list retrieved successfully.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ListTicketsResponse"

    GetTicketSuccess:
      description: Detailed metadata record and nested audit log array loaded successfully.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/GetTicketResponse"

    UpdateTicketStatusSuccess:
      description: State transaction committed and operational audit log recorded successfully.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/SuccessMessageResponse"
          example:
            success: true
            message: Ticket status updated successfully

    ImportTicketEventsSuccess:
      description: Async stream ingestion completed. Partial rejections are outputted via the report payload mapping.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/TicketEventImportResponse"

    GetDailyReportSuccess:
      description: Extracted data metrics mapping loaded successfully.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/DailyReportResponse"

    AITriageSingleBadRequest:
      description: Invalid ticket key data representation constraints.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            invalidId:
              summary: Invalid Input Key
              value:
                success: false
                error:
                  code: INVALID_INPUT
                  message: The target path identifier variable violates mathematical layout logic rules.
            invalidTransitionState:
              summary: Invalid State Flow Boundary
              value:
                success: false
                error:
                  code: INVALID_FLOW_TICKET
                  message: The requested processing target references a ticket residing in a terminal status boundary.

    AITriageTicketNotFound:
      description: Resource verification row scanning yielded zero entities.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: TICKET_NOT_FOUND
              message: The matching ticket index row was not uncovered inside system data partitions.

    AIBatchTriageBadRequest:
      description: Structural array validation anomalies encountered during ingestion processing.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            malformedJson:
              summary: Malformed Request Body
              value:
                success: false
                error:
                  code: INVALID_REQUEST_BODY
                  message: Parsing aborted due to corrupt payload or syntax structure compilation mismatch.
            emptyBatchArray:
              summary: Empty Ingestion Array
              value:
                success: false
                error:
                  code: EMPTY_BATCH
                  message: Execution denied. The batch ingestion ticket identification array contains no items.
            sizeConstraintsExceeded:
              summary: Batch Payload Upper Constraints Exceeded
              value:
                success: false
                error:
                  code: BATCH_TOO_LARGE
                  message: The ingestion tracking sequence limits are exceeded. Reduce the slice payload array footprint.

    AIEvaluationBadRequest:
      description: Validation failures during test dataset sequence binding operations.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            invalidBodyLayout:
              summary: Corrupt JSON Layout
              value:
                success: false
                error:
                  code: INVALID_REQUEST_BODY
                  message: Unable to bind model body entity values due to structural formatting corruption.
            missingPromptMapping:
              summary: Validation Constraints Failure
              value:
                success: false
                error:
                  code: VALIDATION_ERROR
                  message: The tracking field 'prompt_version' is structural and must be explicitly declared.
            emptyCasesMapping:
              summary: Missing Identification Target Array
              value:
                success: false
                error:
                  code: INVALID_INPUT
                  message: Provided evaluation target array identifier sequences must contain active entries.

    BadRequest:
      description: Malformed JSON syntax structure or invalid model validation failure.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: INVALID_REQUEST_BODY
              message: invalid request body

    InvalidQueryParameters:
      description: Provided request parameter arguments failed URL validation parsing rules.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: INVALID_QUERY_PARAMETERS
              message: invalid query parameters

    InvalidTicketId:
      description: The path variable integer does not point to a valid index schema minimum boundary.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: INVALID_INPUT
              message: invalid ticket id

    InvalidStatusTransition:
      description: Requested update violates strict validation constraints governed by the Finite State Machine ruleset.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: INVALID_STATUS_TRANSITION
              message: invalid status transition from new to in_progress

    InvalidImportInput:
      description: Structural integrity checks failed during ingestion due to payload constraint violations.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          examples:
            emptyBody:
              summary: Empty request body
              value:
                success: false
                error:
                  code: EMPTY_REQUEST_BODY
                  message: request body is empty
            unsupportedFileFormat:
              summary: Unsupported file format
              value:
                success: false
                error:
                  code: UNSUPPORTED_FILE_FORMAT
                  message: unsupported file format
            batchTooLarge:
              summary: Batch size exceeds limit
              value:
                success: false
                error:
                  code: BATCH_TOO_LARGE
                  message: batch size exceeds maximum limit

    InvalidDateFormat:
      description: Provided query string argument fails the exact date string layout check requirements.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: INVALID_INPUT
              message: invalid date format, expected YYYY-MM-DD

    InvalidCredentials:
      description: Identity verification failed against Keycloak. Invalid username or password payload.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: UNAUTHORIZED
              message: invalid username or password

    Unauthorized:
      description: Access token verification failed. The Authorization Bearer token is missing, expired, or structural token validation failed.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: UNAUTHORIZED
              message: invalid authorization header format

    Forbidden:
      description: Authenticated subject possesses valid signature claims but lacks the business RBAC roles required for this resource operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: FORBIDDEN
              message: "forbidden: insufficient role"

    TicketNotFound:
      description: Resource resolution failed. No ticket row maps to the provided query index.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: TICKET_NOT_FOUND
              message: ticket not found

    InternalServerError:
      description: The server encountered an unexpected condition which prevented it from fulfilling the request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: INTERNAL_SERVER_ERROR
              message: internal server error

    AuthProviderUnavailable:
      description: Upstream connection timeout or routing failure to Keycloak server endpoint clusters.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
          example:
            success: false
            error:
              code: AUTH_PROVIDER_UNAVAILABLE
              message: authentication provider unavailable

  schemas:
    LoginRequest:
      type: object
      required:
        - username
        - password
      properties:
        username:
          type: string
          example: requestor-1
        password:
          type: string
          format: password
          example: "123123"

    OAuthTokenResponse:
      type: object
      properties:
        access_token:
          type: string
          example: eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...
        refresh_token:
          type: string
          example: eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...
        token_type:
          type: string
          example: Bearer
        expires_in:
          type: integer
          example: 300

    LoginSuccessResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: Login successfully
            data:
              $ref: "#/components/schemas/OAuthTokenResponse"

    CreateTicketRequest:
      type: object
      required:
        - title
        - description
        - priority
        - category
        - sla_due_at
      properties:
        requestor_id:
          type: string
          description: Optional when requestor is derived from authenticated user.
          example: 0a5389df-ba3a-4494-a095-126d05c7c2e7
        title:
          type: string
          example: Cannot access internal system
        description:
          type: string
          example: User cannot access internal support system
        priority:
          $ref: "#/components/schemas/TicketPriority"
        category:
          $ref: "#/components/schemas/TicketCategory"
        sla_due_at:
          type: string
          format: date-time
          example: "2026-05-25T15:57:38.248399+07:00"

    CreateTicketResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: Ticket created successfully
            data:
              type: object
              properties:
                id:
                  type: integer
                  example: 1
                requestor_id:
                  type: string
                  example: 0a5389df-ba3a-4494-a095-126d05c7c2e7
                assignee_id:
                  type: string
                  nullable: true
                  example: 
                title:
                  type: string
                  example: Cannot access internal system
                description:
                  type: string
                  example: User cannot access internal support system
                priority:
                  $ref: "#/components/schemas/TicketPriority"
                category:
                  $ref: "#/components/schemas/TicketCategory"
                status:
                  type: string
                  example: new
                created_at:
                  type: string
                  format: date-time
                  example: "2026-05-15T15:19:55.169302+07:00"
                resolved_at:
                  type: string
                  format: date-time
                  nullable: true
                  example: null
                sla_due_at:
                  type: string
                  format: date-time
                  example: "2026-05-17T15:19:55.169302+07:00"
                cancelled_at:
                  type: string
                  format: date-time
                  nullable: true
                  example: null

    ListTicketsResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: Get tickets successfully
            data:
              $ref: "#/components/schemas/TicketPage"

    GetTicketResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: Get ticket successfully
            data:
              $ref: "#/components/schemas/Ticket"

    SuccessMessageResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: operation completed successfully

    TicketPage:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/Ticket"
        page:
          type: integer
          example: 1
        limit:
          type: integer
          example: 10
        total:
          type: integer
          example: 1
        total_pages:
          type: integer
          example: 1

    Ticket:
      type: object
      properties:
        id:
          type: integer
          example: 1
        requestor_id:
          type: string
          example: 0a5389df-ba3a-4494-a095-126d05c7c2e7
        assignee_id:
          type: string
          nullable: true
          example: 9ee00625-0436-4f11-8bb1-b9e4f3f7bf88
        title:
          type: string
          example: Cannot access internal system
        description:
          type: string
          example: User cannot access internal support system
        priority:
          $ref: "#/components/schemas/TicketPriority"
        category:
          $ref: "#/components/schemas/TicketCategory"
        status:
          type: string
          example: assigned
        created_at:
          type: string
          format: date-time
          example: "2026-05-15T15:19:55.169302+07:00"
        resolved_at:
          type: string
          format: date-time
          nullable: true
          example: null
        sla_due_at:
          type: string
          format: date-time
          example: "2026-05-17T15:19:55.169302+07:00"
        cancelled_at:
          type: string
          format: date-time
          nullable: true
          example: null
        events:
          type: array
          items:
            $ref: "#/components/schemas/TicketEvent"

    TicketStatus:
      type: string
      enum:
        - new
        - assigned
        - in_progress
        - resolved
        - closed
        - cancelled
      example: new

    TicketPriority:
      type: string
      enum:
        - low
        - medium
        - high
      example: low

    TicketCategory:
      type: string
      enum:
        - IT
        - HR
        - Facilities
      example: IT

    UpdateTicketStatusRequest:
      type: object
      required:
        - status
      properties:
        status:
          $ref: "#/components/schemas/TicketStatus"
        note:
          type: string
          example: Start processing ticket

    TicketEvent:
      type: object
      properties:
        id:
          type: integer
          example: 1
        ticket_id:
          type: integer
          example: 1
        from_status:
          $ref: "#/components/schemas/TicketStatus"
        to_status:
          type: string
          example: assigned
        requestor_id:
          type: string
          nullable: true
          example: 0a5389df-ba3a-4494-a095-126d05c7c2e7
        assignee_id:
          type: string
          nullable: true
          example: 9ee00625-0436-4f11-8bb1-b9e4f3f7bf88
        note:
          type: string
          example: Ticket assigned to support agent
        created_at:
          type: string
          format: date-time
          example: "2026-06-04T17:59:49.129094074+07:00"

    TicketEventImportFileRequest:
      type: object
      required:
        - file
      properties:
        file:
          type: string
          format: binary
          description: CSV or JSON file containing historical state trails to parse.

    TicketEventImportJsonRequest:
      type: array
      items:
        $ref: "#/components/schemas/TicketEventImportItem"

    TicketEventImportItem:
      type: object
      required:
        - ticket_id
        - from_status
        - to_status
        - created_at
      properties:
        ticket_id:
          type: integer
          example: 19
        note:
          type: string
          example: Ticket assigned to support agent
        from_status:
          type: string
          nullable: true
          example: assigned
        to_status:
          type: string
          nullable: true
          example: assigned
        assignee_id:
          type: string
          nullable: true
          example: 9ee00625-0436-4f11-8bb1-b9e4f3f7bf88
        created_at:
          type: string
          format: date-time
          example: "2026-05-30T17:59:49.129094074+07:00"

    TicketEventImportResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/TicketEventImportResult"

    TicketEventImportResult:
      type: object
      properties:
        accepted_count:
          type: integer
          example: 0
        rejected_count:
          type: integer
          example: 1
        duplicate_count:
          type: integer
          example: 1
        rejected_details:
          type: array
          items:
            $ref: "#/components/schemas/TicketEventImportItem"

    TicketEventRejectedGroup:
      type: object
      properties:
        error_name:
          type: string
          example: from_status and to_status cannot be the same 'assigned'
        events:
          type: array
          items:
            $ref: "#/components/schemas/TicketEventImportItem"

    DailyReportResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/DailyReport"

    DailyReport:
      type: object
      properties:
        id:
          type: integer
          example: 3
        report_date:
          type: string
          format: date-time
          example: "2026-05-22T00:00:00Z"
        new_count:
          type: integer
          example: 45
        resolved_count:
          type: integer
          example: 38
        cancelled_count:
          type: integer
          example: 3
        overdue_count:
          type: integer
          example: 12
        avg_resolution_time:
          type: integer
          example: 120
        high_priority_count:
          type: integer
          example: 5
        medium_priority_count:
          type: integer
          example: 25
        low_priority_count:
          type: integer
          example: 15
        sla_breache_count:
          type: integer
          example: 2
        created_at:
          type: string
          format: date-time
          example: "2026-05-22T16:03:47.639267008+07:00"

    AITriageResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: AI triage completed successfully
            data:
              $ref: "#/components/schemas/AITriageResult"

    AIBatchTriageRequest:
      type: object
      required:
        - ticket_ids
      properties:
        ticket_ids:
          type: array
          items:
            type: integer
          example: [455, 456, 457, 458]

    AIBatchTriageResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/BatchTriageResult"
          example:
            success: true
            data:
              processed:
                - ticket_id: 455
                  category: "IT"
                  urgency_level: "low"
                  sla_breach_risk: "low"
                  reason_summary: "The ticket requests an iPhone X for QA testing, a clear IT device procurement request. No event history indicates work is already underway, so the urgency remains low. The SLA time left is 91 hours, which is not short, and there is no backlog information, so the risk of SLA breach is low. Confidence is high due to the clear domain keywords and lack of ambiguity."
                  recommended_next_action: "Assign to IT device procurement team to check availability and arrange delivery"
                  confidence_score: 0.9
                  fallback_used: false
                  prompt_version: "v1.1"
              failed:
                - ticket_id: 456
                  reason: "ticket is overdue and cannot be triaged"
                - ticket_id: 457
                  reason: "ticket not found"

    BatchTriageResult:
      type: object
      required:
        - processed
        - failed
      properties:
        processed:
          type: array
          items:
            $ref: "#/components/schemas/AITriageResult"
        failed:
          type: array
          items:
            $ref: "#/components/schemas/BatchTriageFailedItem"

    BatchTriageFailedItem:
      type: object
      required:
        - ticket_id
        - reason
      properties:
        ticket_id:
          type: integer
          description: Unique identifier of the ticket that failed validation.
          example: 24
        reason:
          type: string
          description: Reason why the ticket could not be triaged.
          example: ticket is overdue and cannot be triaged

    AITriageResult:
      type: object
      required:
        - ticket_id
        - category
        - urgency_level
        - sla_breach_risk
        - reason_summary
        - recommended_next_action
        - confidence_score
        - prompt_version
        - fallback_used
      properties:
        ticket_id:
          type: integer
          description: The unique identifier of the analyzed support ticket resource.
          example: 455
        category:
          type: string
          description: Classified functional organizational domain assignment.
          example: IT
        urgency_level:
          type: string
          description: Evaluated internal operational resolution priority weighting.
          example: high
        sla_breach_risk:
          type: string
          description: Predicted algorithmic probability metric of violating system SLA markers.
          example: high
        reason_summary:
          type: string
          description: Concise natural language explanation details supporting the logical triage deduction.
          example: "This ticket has been stalled In Progress status for 5 consecutive hours with no recent activity logs. Based on historical resolution times, the assigned agent is likely facing a technical roadblock. An SLA breach will occur in less than 45 minutes if not intervened."
        recommended_next_action:
          type: string
          description: Tailored operations instruction proposal for human workflow operators.
          example: "Please initiate an immediate supervisor intervention. I recommend assigning a secondary senior engineer to co-own this ticket with the primary agent to unblock the technical issue and prevent a breach."
        confidence_score:
          type: number
          format: float
          description: Model verification scoring matrix mapping parameter.
          example: 0.92
        prompt_version:
          type: string
          description: Active context pipeline semantic version tracing key.
          example: v1.2.0
        fallback_used:
          type: boolean
          description: Explicit flag denoting if automated heuristic fallback baselines stepped in.
          example: false

    AIEvaluationRequest:
      type: object
      required:
        - prompt_version
        - evaluation_case_ids
      properties:
        prompt_version:
          type: string
          description: Target engineering prompt pipeline tag identifier to evaluate against ground truth datasets.
          example: "v2.1.0-triage-assistant"
        evaluation_case_ids:
          type: array
          items:
            type: integer
          description: Array collection filtering specific seed case IDs to test within this session execution.
          example: [1, 2, 3, 4, 5, 12, 20]

    AIEvaluationResponse:
      allOf:
        - $ref: "#/components/schemas/SuccessResponseBase"
        - type: object
          properties:
            message:
              type: string
              example: Evaluation run completed successfully
            data:
              $ref: "#/components/schemas/AIEvaluationData"
          example:
            success: true
            message: Evaluation run completed successfully
            data:
              run_id: 17
              run_date: "2026-06-12T10:23:31.390636683+07:00"
              prompt_version: "v1.2"
              metrics:
                accuracy:
                  total_cases: 1
                  passed_cases: 1
                  failed_cases: 0
                  accuracy_rate: 100
                performance:
                  total_duration_ms: 6389
                  avg_latency_ms: 6389
                  throughput_cps: 0.15651901706057286
              detail_raw:
                - test_case_id: 2
                  actual_category: "HR"
                  actual_urgency: "low"
                  actual_sla_breach_risk: "low"
                  is_overall_passed: true
                  risk_explanation: "The request is for a performance evaluation template, which falls under HR. The ticket priority is low, and the SLA has 97 hours remaining, giving ample time. The issue is straightforward and not complex, and there is no backlog, so the risk of SLA breach is low."
                  latency_ms: 6389

    AIEvaluationData:
      type: object
      required:
        - run_id
        - run_date
        - prompt_version
        - metrics
        - detail_raw
      properties:
        run_id:
          type: integer
          description: Auto-incrementing relational tracking index assigned to this evaluation job pipeline.
          example: 17
        run_date:
          type: string
          format: date-time
          description: Generated system timestamp layout recording exact execution start window.
          example: "2026-06-12T10:23:31.390636683+07:00"
        prompt_version:
          type: string
          description: The evaluated template config state tracking label.
          example: "v1.2"
        metrics:
          $ref: "#/components/schemas/AIEvaluationMetrics"
        detail_raw:
          type: array
          description: Detailed array tracking individual test cases from the evaluation sequence execution.
          items:
            $ref: "#/components/schemas/AIEvaluationDetailRaw"

    AIEvaluationMetrics:
      type: object
      required:
        - accuracy
        - performance
      properties:
        accuracy:
          type: object
          required:
            - total_cases
            - passed_cases
            - failed_cases
            - accuracy_rate
          properties:
            total_cases:
              type: integer
              example: 1
            passed_cases:
              type: integer
              example: 1
            failed_cases:
              type: integer
              example: 0
            accuracy_rate:
              type: number
              example: 100
        performance:
          type: object
          required:
            - total_duration_ms
            - avg_latency_ms
            - throughput_cps
          properties:
            total_duration_ms:
              type: integer
              example: 6389
            avg_latency_ms:
              type: integer
              example: 6389
            throughput_cps:
              type: number
              format: float
              example: 0.15651901706057286

    AIEvaluationDetailRaw:
      type: object
      required:
        - test_case_id
        - actual_category
        - actual_urgency
        - actual_sla_breach_risk
        - is_overall_passed
        - risk_explanation
        - latency_ms
      properties:
        test_case_id:
          type: integer
          example: 2
        actual_category:
          type: string
          example: "HR"
        actual_urgency:
          type: string
          example: "low"
        actual_sla_breach_risk:
          type: string
          example: "low"
        is_overall_passed:
          type: boolean
          example: true
        risk_explanation:
          type: string
          example: "The request is for a performance evaluation template, which falls under HR. The ticket priority is low, and the SLA has 97 hours remaining, giving ample time. The issue is straightforward and not complex, and there is no backlog, so the risk of SLA breach is low."
        latency_ms:
          type: integer
          example: 6389
          
    SuccessResponseBase:
      type: object
      properties:
        success:
          type: boolean
          example: true

    ErrorResponse:
      type: object
      properties:
        success:
          type: boolean
          example: false
        error:
          $ref: "#/components/schemas/ErrorDetail"

    ErrorDetail:
      type: object
      properties:
        code:
          type: string
          example: INVALID_INPUT
        message:
          type: string
          example: invalid input

security:
  - OAuth2Password: []